import type { Location, LocationCardVM } from "@/types";

// Demo locations removed – all location data now lives in Supabase.
export const mockLocations: Location[] = [];

export const mockLocationCards: LocationCardVM[] = [];
